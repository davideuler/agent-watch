# Release Stability Evaluation（发布稳定性评分说明）

Agent Watch 对每个版本进行 **0–10 分的稳定性评级**（10分最稳定，0分最不稳定），基于 GitHub issue 数据、LLM 分类和社区评分综合计算。

---

## 评级档位

| 分数 | 等级 | 含义 |
|---|---|---|
| 8.2 – 10 | **STABLE（稳定）** | 观测到的发布风险极低 |
| 6.8 – 8.1 | **MOSTLY STABLE（基本稳定）** | 有问题但无大范围故障 |
| 5.2 – 6.7 | **MIXED（混合）** | 有足够风险信号，升级前需检查相关工作流 |
| 3.5 – 5.1 | **RISKY（有风险）** | 存在核心或大范围故障 |
| 0 – 3.4 | **UNSTABLE（不稳定）** | 严重/大范围/核心问题占主导 |
| 5.0（灰色）| **COLLECTING SIGNAL（收集信号中）** | 发布后前 3 小时 |

---

## 第一步 — 新版本宽限期

发布后 **3 小时内**，Agent Watch 返回中性分数 **5**，状态标记为 *分析中*，不进行评分计算。

---

## 第二步 — 单个 Issue 的风险权重

每个与该版本关联的负面 GitHub issue 计算一个原始风险权重：

```
原始权重 = 时效性 × 讨论热度 × 重复集群 × 置信度
           × 严重性权重 × 影响范围权重 × 功能领域权重 × 受影响用户比例权重 × 变通方案权重
```

**每个 issue 最高贡献值上限为 5**（`PER_ISSUE_CAP = 5`），单个极端 issue 不能独自拉垮分数。

### 各因子权重

| 因子 | 取值 → 权重 |
|---|---|
| **严重性（Severity）** | critical → 2.8 · high → 1.9 · medium → 1.0 · low → 0.35 |
| **影响范围（Impact scope）** | broad（大范围）→ 1.75 · moderate（中等）→ 1.0 · niche（小众）→ 0.3 |
| **功能领域（Functionality）** | core（核心）→ 1.65 · provider → 0.75 · integration → 0.45 · docs → 0.15 · unknown → 0.8 |
| **受影响用户比例** | many（大量）→ 1.45 · some（部分）→ 0.9 · few（少数）→ 0.35 · unknown → 0.75 |
| **变通方案状态** | none（无）→ 1.0 · unknown → 0.85 · partial（部分）→ 0.65 · confirmed（已确认）→ 0.35 |

### 时效性衰减（每个 issue 独立计算）
```
时效性 = 0.55 + 0.45 × exp(−距今天数 / 45)
```
新鲜 issue 权重最高；超过约 45 天的 issue 衰减至 0.55。

### 讨论热度加成
```
讨论热度 = 1 + min(1.4, log10(1 + 评论数) × 0.45)
```
高评论量表明问题影响面更广。

### 重复集群加成
```
重复集群 = 1 + log2(集群大小) × 0.28
```
重复集群越大，说明问题可复现性更强。

---

## 第三步 — 核心严重问题与其他问题分离

所有负面 issue 分为两类：

- **核心严重（Core-serious）**：`功能领域 == core` 且（`严重性 == critical` 或 `严重性 == high`）
- **其他问题（Other）**：其余所有问题（小众问题、provider/integration bug、低严重性 core 问题等）

正面 issue 产生**正面预算**用于抵消负面权重：
```
正面预算 = 0.7 × 正面权重合计
```

正面预算优先抵消"其他问题"，剩余预算再抵消"核心严重问题"：
```
有效核心权重  = max(0, 核心严重原始权重 − 正面抵消部分)
有效其他权重  = max(0, 其他原始权重 − 正面抵消部分)
```

---

## 第四步 — 评分公式

### 核心分数（主要驱动因素）
```
核心风险指数 = 有效核心权重
核心分数     = 10 / (1 + (核心风险指数 / 4.2) ^ 1.35)
```

- 1个 core+critical 或 core+high bug（权重约 4–5）→ 核心分数约 **4–5 分**（Risky 档）
- 无核心严重问题 → 核心分数为 **5 分**（中性基准）

### 其他问题扣分（最多扣 2 分）
```
其他扣分 = 2.0 × (1 − exp(−有效其他权重 / 3.0))
```

无论存在多少非核心严重问题，**合计最多只扣 2 分**。

### 基础分数
```
基础分数 = max(1.0, 核心分数 − 其他扣分)
```

硬性下限为 **1.0 分**（非灰色状态的最低分）。

---

## 第五步 — 社区评分融合

若用户已提交评分（1–10 星）：
```
评分均值   = 所有提交评分的平均值
评分权重   = clamp(评分数 / (评分数 + 5), 0, 0.60)
最终分数   = 基础分数 × (1 − 评分权重) + 评分均值 × 评分权重
```

当评分数量足够多（≥5 条）时，社区评分最高占 **60% 权重**。

---

## 第六步 — 同版本组横向兜底（Peer Floor）

若某版本的总负面权重（核心严重 + 其他）**不超过同项目所有版本的中位数**，且当前分数低于 **5.5 分**，则自动将分数托底至 **5.5 分**。

此机制防止信号量平均或优于大多数版本的发布因绝对权重偏低而被误判为低分。

---

## 分数解读速查

| 问题情况 | 典型分数范围 |
|---|---|
| 无负面 issue | 5.0（中性） |
| 仅有小众/低严重性问题 | 3.0 – 5.0（基准最多扣 2 分） |
| 1 个 core+critical/high bug | 3.5 – 5.5（Risky / Mixed） |
| 2–3 个 core+critical/high bug | 2.0 – 4.0（Risky / Unstable） |
| 大量 core+critical bug | 1.0 – 2.5（Unstable） |
| 社区正面评分较高 | 最多 +3 分（融合提升） |
| 负面信号低于同组中位数 | 托底至 5.5 分 |

---

## 更新频率

评分每 **20 分钟**通过 cron 重新计算，从 GitHub issues 和 LLM 分类中拉取最新数据。

历史版本（距最新版本 2 个及以上版本）：若 GitHub issue 和用户评分数据未发生变化，则冻结分数，不再重算。
